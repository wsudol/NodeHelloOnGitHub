---
applications:
- name: NodeHelloWorldGit
  services:
  - mycache-DataCache
  memory: 128M
  instances: 1
  path: .
  command: node app.js

declared-services:
  mycache-DataCache:
    label: DataCache
    plan: free
